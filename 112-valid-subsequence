public Boolean isSubSequence(String s1, String s2){
    //code here
Integer pointer=0;
    for(Integer i=0;i<s1.length();i++){

          for(Integer j=0;i<s2.length();i++){
              
            
              if(s1.charAt(i)==s2.charAt(j)){
                   if(j>pointer) 
                  {pointer=j;}
                  else{
                      return false;
                  }
              }
          }
    }
    return true;
}
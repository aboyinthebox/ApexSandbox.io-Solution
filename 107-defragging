public void defragging(Integer[] memory)
{
    // code here

 int i = 0;
    int j = 0;
    int n = arr.size();
    while (j < n) {
        if (arr.get(j) != -1) {
            arr.set(i, arr.get(j));
            i++;
        }
        j++;
    }
    while (i < n) {
        arr.set(i, -1);
        i++;
    }
}